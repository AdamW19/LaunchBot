# import glob
# import os

# what this did is to rename all of the stage images from splatoonwiki to something we want

FILE_PREFIX = "./img/stages/"
# list_of_files = glob.glob(FILE_PREFIX + "*.png")

# for file in list_of_files:
    # new_filename = file[15:]  # removes "S2-Stage-"
    # new_filename = new_filename.replace("_", "-")
    # new_filename = FILE_PREFIX + new_filename
    # os.rename(file, new_filename)

# print("Done")
